# Builder-code findings (Shannon testnet)

Generated by `pnpm builder-matrix` on 2026-09-08T23:42:36.303Z.
All results are `eth_call` simulations of `placeBinaryOrder` from the funded address
`0xBe9Ad0286CBDfdcbCbf776262C195d0338999DA9`, IOC BUY_YES at the best ask, 1 contract, on a live Trading market of the
DreamDEX venue. Real sends: ERC-20 approve to the pool (0x50b0b49968b4b76ab391c49399370e2ba8575708c53ff769d90d15b937c3eb31); approveBuilder(PARTNER, 0) (0x4eb1d57d06d409f709d0f4f1b2c698724d0124d8a6937d26b0d64902ac671d30).

| | value |
| --- | --- |
| market | `0x00000000000000000000000000000000000000000000000000000000000177a2` — BTC 300s, expiry 2026-09-08T23:45:00.000Z |
| pool | `0x4143f9C602547a3159BbAE017395025F98752518` |
| pool `getMaxBuilderFeeBpsTimes1k()` | **0** |
| PARTNER (throwaway builder address) | `0xb5eCf004491aa8589a82af91633D18867fcFF038` |
| userData (Relay v1 tag, partner 1, surface WEB) | `72057594054705408` (0x100000001000100) |
| order | kind BUY_YES, price 355000 (0.355), qty 1000000, IOC, expire 1788910993000000000 ns |

## Matrix

| variant | builder | builderFeeBpsTimes1k | approveBuilder | result | selector |
| --- | --- | --- | --- | --- | --- |
| A | address(0) | 0 | no | OK success=true orderId=258254417031934195572 | - |
| B | PARTNER | 0 | no | OK success=true orderId=258254417031934195572 | - |
| C | PARTNER | 0 | sent (cap 0) | OK success=true orderId=258254417031934195574 | - |
| D | PARTNER | 1 | no | REVERT BuilderFeeExceedsCap | 0xf559e808 |

C detail: approveBuilder(PARTNER, 0) SENT 0x4eb1d57d06d409f709d0f4f1b2c698724d0124d8a6937d26b0d64902ac671d30 status=success gasUsed=61101; getBuilderApproval=0 getEffectiveBuilderApproval=0

E detail: approveBuilder(PARTNER, 1) simulation: REVERT BuilderFeeExceedsCap [0xf559e808]

## Real sends (Step 4) and the `BuilderFeeCharged` event

Both variants were then sent for real, one contract each, on the same market:

| variant | tx | fillPrice | qty | extra pool log |
| --- | --- | --- | --- | --- |
| A `builder=0` | `0x961dcefd13618cb7f114284c74bf7d0498f852f44239a95fc4ca5e04dd51732c` | 0.344 | 1 | none |
| B `builder=PARTNER, fee 0` | `0x0daeba756eb106bf1b4ceb455f04390b76c1a54e9357330484ef468f4f6b7d0d` | 0.338 | 1 | **`BuilderFeeCharged`** |

The B receipt carries one log A does not, emitted by the pool right after `BinaryOrderPlaced`:
topic0 `0xb603f98363ba2c49dc586ce0aa14affc3f62a5df0b81fa84c6bfb96ec4eccc93`, three indexed topics
`(orderId, PARTNER, tUSDC)` and one data word `0`. Matched by keccak to

```solidity
event BuilderFeeCharged(uint128 indexed orderId, address indexed builder, address indexed token, uint256 amount);
```

So the pool records the builder attribution **even when the fee is 0**. There is no payer field
(join `orderId` → `OrderPlaced.owner`) and no `isTakerSide`. It is now in `@relay/core`
`observedEventsAbi` and `TOPICS.BuilderFeeCharged`.

## Second cycle (hedged, to exercise redeem)

Market `0x…177b0` (BTC 15 m, expiry 2026-09-09T00:00:00Z), one contract each:

| leg | kind | tx | limit (own) | paid (own) | notes |
| --- | --- | --- | --- | --- | --- |
| A UP, builder 0 | BUY_YES | `0x743321c27567123b860e590b64a3bbcf4eba1713994532de37fade90c5e7e648` | 0.435 | **0.315** | escrowed 0.435, refunded 0.120 in-tx: the taker pays the maker's resting price |
| B DOWN, builder PARTNER | BUY_NO | `0x250d902bc41d7b8b2ea35b2363114573c0f1cc545caa10e907a42a1e513d8d52` | 0.718 | **0.713** | `BuilderFeeCharged(orderId, PARTNER, tUSDC, 0)` again; fill emitted `SetMinted` — the NO buy crossed a YES buy and minted a fresh pair (YES → maker, NO → us) |

Also learned: an IOC that finds nothing to cross **reverts** `ImmediateOrCancelNoFill()`
(`0xd48c4403`) rather than mining as a no-op — seen when the touch moved between the book read and
the send. Relay prices 5 ticks through the touch and retries once on a fresh book.

## Reading

- **A (baseline)** is the path Relay always has: untagged builder, Relay `userData`. Accepted.
- **B**: builder ≠ 0 with fee 0 and NO approval is **ACCEPTED** on a cap-0 testnet pool. So the builder address can be attached on testnet at no cost; `approveBuilder` only gates a non-zero fee.
- **C**: approveBuilder(PARTNER, 0) is a valid pool call even at cap 0; the tagged fee-0 order afterwards: OK success=true orderId=258254417031934195574.
- **D**: fee = 1 on a cap-0 pool → REVERT BuilderFeeExceedsCap. This is the error the widget must map to "builder fees are not enabled on this network".
- Open: whether an approval on this pool survives the pool's recycle onto its next market (nonce+1), and the exact `BuilderFeeCharged` shape — a real fee needs mainnet (cap 1 %).
