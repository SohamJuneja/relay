packages:
  - "packages/*"
  - "apps/*"

# markets-sdk declares react as a peer; this repo has no React yet.
autoInstallPeers: false
strictPeerDependencies: false

# esbuild (via tsx) needs its postinstall to place the platform binary.
allowBuilds:
  esbuild: true
