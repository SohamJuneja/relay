{
  "name": "@relay/sdk",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Relay for agents: place attributed orders on DreamDEX Event Contracts from Node, tagged with your partner id and builder code.",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "example:momentum": "tsx examples/momentum-bot.ts"
  },
  "dependencies": {
    "@relay/core": "workspace:*",
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "dotenv": "^16.4.5",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
