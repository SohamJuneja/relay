{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd ../.. && corepack enable && pnpm install --frozen-lockfile --ignore-scripts && pnpm --filter @relay/console build",
  "outputDirectory": "dist",
  "installCommand": "echo 'installed by buildCommand at the workspace root'",
  "framework": null,
  "rewrites": [
    {
      "source": "/((?!assets/).*)",
      "destination": "/index.html"
    }
  ]
}
