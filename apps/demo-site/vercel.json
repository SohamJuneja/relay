{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd ../.. && corepack enable && pnpm install --frozen-lockfile --ignore-scripts && pnpm --filter @relay/embed build && pnpm --filter @relay/demo-site build",
  "outputDirectory": "dist",
  "installCommand": "echo 'installed by buildCommand at the workspace root'",
  "framework": null
}
