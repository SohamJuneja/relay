{
  "name": "@relay/demo-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Block Ledger — a fictional crypto publication with the Relay widget embedded, to prove the widget lives inside a third party's design.",
  "scripts": {
    "prebuild": "node scripts/sync-widget.mjs",
    "predev": "node scripts/sync-widget.mjs",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 4181",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@relay/embed": "workspace:*",
    "@types/node": "^20.14.0",
    "playwright": "^1.63.0",
    "typescript": "^5.6.0",
    "vite": "^6.3.5"
  }
}
